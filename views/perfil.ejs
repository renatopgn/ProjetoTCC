<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Meu Perfil - Academia</title>
  <link rel="stylesheet" href="/css/meuperfil.css">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
</head>
<body>

  

  <%- include('partials/navbar') %>

  <!-- CONTEÚDO DA PÁGINA -->
  <main class="perfil-container">
    <!-- ESQUERDA -->
    <section class="perfil-lado-esquerdo" id="usuario-info"> 
      <h2>Seus Dados Pessoais</h2>

      <div class="info-usuario campo-usuario">
        <label>Nome</label>
        <span><%= user.nome %></span>
        <input type="text" value="<%= user.nome %>" name="nome" style="display: none;">
      </div>

      <div class="info-usuario campo-usuario">
        <label>Senha</label>
        <span>********</span>
        <div class="senha-wrapper" style="display: none; align-items: center;">
          <input type="password" name="senha" id="senha-campo" placeholder="Nova senha (deixe em branco para manter atual)" style="width: calc(100% - 40px);">
          <button type="button" id="toggleSenha" style="width: 36px; margin-left: 4px; cursor: pointer;">👁️</button>
        </div>
      </div>

      <div class="info-usuario campo-usuario">
        <label>Data de Nascimento</label>
        <span><%= user.data_nasc %></span>
        <input type="date" value="<%= user.data_nasc %>" name="data_nasc" style="display: none;">
      </div>

     <div class="info-usuario campo-usuario">
  <label>CPF</label>
  <span><%= user.cpf || '' %></span>
  <input type="text" value="<%= user.cpf || '' %>" name="cpf" id="cpf-input" maxlength="14" style="display: none;">
</div>

      <div class="info-usuario campo-usuario">
  <label>Telefone</label>
  <span><%= user.contato || '' %></span>
  <input type="tel" value="<%= user.contato || '' %>" name="contato" maxlength="15" style="display: none;">
</div>

      <div class="botoes-usuario">
        <button id="editar-dados">Editar</button>
        <button id="salvar-dados" style="display: none;">Salvar</button>
        <button id="cancelar-dados" style="display: none;">Cancelar</button>
      </div>
    </section>

    <!-- DIREITA: FORMULÁRIO -->
    <section class="formulario-perfil">
      <h2>Ficha de Avaliação</h2>
      <form id="form-avaliacao" method="POST" action="/perfil/update" enctype="multipart/form-data">
        <div class="campo">
          <label>Altura</label>
          <input type="text" name="altura" value="<%= user.altura || '' %>" placeholder="Ex: 1,75 m" disabled>
        </div>
        <div class="campo">
          <label>Peso</label>
          <input type="text" name="peso" value="<%= user.peso || '' %>" placeholder="Ex: 68 kg" disabled>
        </div>
        <div class="campo">
          <label>Medicação (se houver)</label>
          <input type="text" name="medicacao" value="<%= user.medicacao || '' %>" placeholder="Digite se faz uso de medicamentos" disabled>
        </div>

         <div class="campo">
          <label>Comorbidade</label>
          <input type="text" name="comorbidade" value="<%= user.comorbidade || '' %>" placeholder="Ex: Diabetes..." disabled>
        </div>

        <div class="campo">
          <label>Objetivo</label>
          <input type="text" name="objetivo" value="<%= user.objetivo || '' %>" placeholder="Ex: Emagrecer..." disabled>
        </div>
       
       
        <div class="campo">
          <label>Motivo</label>
          <textarea name="motivo" placeholder="Por que está buscando treinos personalizados?" disabled><%= user.motivo || '' %></textarea>
        </div>

        <div class="botoes-formulario">
          <button type="button" id="editar">Editar</button>
          <button type="submit" id="salvar" style="display: none;">Salvar</button>
        </div>
      </form>
    </section>
  </main>

  <script src="/js/meuperfil.js"></script>
</body>
</html>